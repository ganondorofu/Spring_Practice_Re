spring.application.name=demo

# MySQLデータベースの接続URL
spring.datasource.url=jdbc:mysql://35.222.160.100/ganon?useSSL=false&serverTimezone=UTC

# MySQLデータベースのユーザー名
spring.datasource.username=root

# MySQLデータベースのパスワード
spring.datasource.password=Stemask1234

# 使用するJDBCドライバのクラス名（通常自動設定されますが、明示的に指定しても構いません）
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPAの自動DDL生成設定（テーブルの作成・更新）
spring.jpa.hibernate.ddl-auto=update

# SQLクエリをコンソールに表示
spring.jpa.show-sql=true

# logging.level.root=DEBUG
